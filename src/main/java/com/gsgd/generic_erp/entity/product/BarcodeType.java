package com.gsgd.generic_erp.entity.product;

/** Symbology used by a product barcode. */
public enum BarcodeType {
    /** EAN-13 barcode. */
    EAN13,
    /** EAN-8 barcode. */
    EAN8,
    /** UPC-A barcode. */
    UPC_A,
    /** UPC-E barcode. */
    UPC_E,
    /** Code 128 barcode. */
    CODE128,
    /** QR code. */
    QR
}
